# 공용 계약

팀 공용 인터페이스와 지역 마스터를 둡니다. `data/analysis/COLLABORATION.md`의 `PeerFinder`,
`PerformanceEvaluator`, `GapAnalyzer` 계약을 이 디렉터리에 구현합니다.

현재 분석 패키지는 이전 코드의 재배치를 우선 완료한 상태입니다. 전국 `region_id`
마스터와 Protocol 구현이 확정되면 이 디렉터리를 독립 Python 패키지로 전환합니다.
