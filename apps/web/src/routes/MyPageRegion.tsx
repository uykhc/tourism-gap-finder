import RoutePlaceholder from '../components/RoutePlaceholder';

function MyPageRegion() {
  return (
    <RoutePlaceholder
      title="관심 지역 설정"
      description="관심 지역 설정 화면을 준비하고 있습니다."
    />
  );
}

export default MyPageRegion;
