import RoutePlaceholder from '../components/RoutePlaceholder';

function RegionComparison() {
  return (
    <RoutePlaceholder
      title="지역 비교"
      description="지역 비교 화면을 준비하고 있습니다."
    />
  );
}

export default RegionComparison;
